# Runtime dependencies actually imported by server.py, used to build the
# offline install bundle (deploy/offline_bundle.sh) and the production venv
# (deploy/install.sh). This is a small subset of requirements.txt on purpose:
# requirements.txt also pins dev/test tooling and unrelated SDKs (pytest,
# black, mypy, boto3, openai, google-generativeai, litellm, ...) that server.py
# never imports. Bundling those for an air-gapped install only adds wheel-
# availability risk (some aren't even on public PyPI) with no runtime benefit.
#
# Keep versions here in sync with the matching pins in requirements.txt.
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
pydantic==2.13.4
email-validator==2.3.0
python-multipart==0.0.32
python-dotenv==1.2.2
motor==3.3.1
pymongo==4.6.3
dnspython==2.8.0
bcrypt==4.1.3
PyJWT==2.13.0
qrcode==8.2
pillow==12.2.0
